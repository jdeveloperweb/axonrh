spring:
  main:
    web-application-type: reactive
  data:
    redis:
      host: localhost
      port: 6379

jwt:
  secret: test-jwt-secret-key-for-testing-purposes-only

logging:
  level:
    root: WARN
    com.axonrh: DEBUG
